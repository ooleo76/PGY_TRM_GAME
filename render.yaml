services:
  - type: web
    name: ihca
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ROOM
        value: "2468"        # 固定房間代碼，之後可在 Render 後台改
      - key: TKEY
        value: "emed2026"    # 教師金鑰，請改成你自己的
