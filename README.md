# IHCA 團隊復甦模擬器 v3.1 — 多人版

給急診 UGY / PGY 的 TRM（團隊資源管理）教學用。
學員各拿一支手機，投影幕放全景，教師另有一台控制台。
全程資訊不對稱：**你查到的事、你摸到的脈搏，不講出來別人就不知道。**

---

## 目錄

1. [檔案](#檔案)
2. [三種上線方式，挑一個](#三種上線方式挑一個)
3. [上課流程](#上課流程)
4. [手機怎麼操作](#手機怎麼操作)
5. [重播](#重播)
6. [教案設計重點](#教案設計重點)
7. [想改內容的話](#想改內容的話)

---

## 檔案

| 檔案 | 用途 |
|---|---|
| `server.js` | 伺服器（零外部套件，只用 Node 內建模組） |
| `ihca-sim.js` | 模擬核心，伺服器與瀏覽器共用 |
| `qr.js` | QR code 產生器（自己寫的，不需要連外） |
| `ihca-client.html` | 客戶端，同一個檔案切換五種模式 |
| `package.json` / `Dockerfile` / `render.yaml` / `fly.toml` | 雲端部署用 |
| `recordings/` | 錄影檔自動存在這裡 |

事前只要裝一次 **Node.js**（<https://nodejs.org> 下載 LTS 版，一直按下一步）。

---

## 三種上線方式，挑一個

### A. 臨時公開網址（最快，不用註冊任何帳號）★ 建議先用這個

**適用你的情境**：桌機接網路線、學員用院內加密 WiFi，兩邊不同網路。

先裝一次 cloudflared（只要一次）：

```
Windows :  winget install --id Cloudflare.cloudflared
macOS   :  brew install cloudflared
```

上課時：

```
node server.js --tunnel
```

終端機會印出一個 `https://xxxx.trycloudflare.com` 開頭的公開網址。
學員的手機不管接哪個 WiFi、甚至用 4G，都連得到。

> 網址每次執行都不一樣 —— 但學員是**掃 QR code** 進來的，所以不影響。
> 關掉終端機，網址就失效，不會留著。

### B. 固定網址（部署到 Render，免費）

適合「每次上課都是同一個網址」、或你不想在上課時多開一個終端機。

1. 到 <https://github.com> 註冊（如果還沒有帳號），建一個新的 repository，
   把這個資料夾裡的檔案全部上傳（網頁上直接拖曳就可以）。
2. 到 <https://render.com> 用 GitHub 帳號登入 → **New +** → **Web Service**
   → 選剛剛那個 repository。
3. 設定頁面填：
   - **Runtime**：Node
   - **Build Command**：留空
   - **Start Command**：`node server.js`
   - **Instance Type**：Free
4. 展開 **Advanced** → **Add Environment Variable**，加兩個：

   | Key | Value |
   |---|---|
   | `ROOM` | 你要的四位數房間代碼，例如 `2468` |
   | `TKEY` | 你自己想一組英數字當教師金鑰，例如 `emed2026` |

5. 按 **Create Web Service**，等一兩分鐘，會拿到一個像
   `https://ihca-xxxx.onrender.com` 的固定網址。

以後上課只要開：

* 投影幕 `https://你的網址/?m=host&k=emed2026`
* 教師 　 `https://你的網址/?m=teacher&k=emed2026`
* 學員 　 掃投影幕上的 QR code

> **免費方案會睡著**：15 分鐘沒人用會休眠，下次開要等 30–60 秒醒來。
> 上課前 5 分鐘先開一下投影幕那個網址就好。
> 另外免費方案的硬碟是暫時的，錄影檔請在課後用教師控制台的「下載錄影檔」帶走。

（`render.yaml`、`Dockerfile`、`fly.toml` 都已經放在資料夾裡，
想改用 Fly.io 或其他平台也可以直接吃。）

### C. 同一個 WiFi（最單純，但你的情境用不到）

```
node server.js
```

終端機會印出 `http://192.168.x.x:8080/?r=代碼`，手機連同一個 WiFi 就能開。

---

## 上課流程

### 三個畫面

| 誰 | 開哪個網址 | 看得到什麼 |
|---|---|---|
| **投影機** | `…/?m=host&k=金鑰` | 大廳時是 QR code 與房間代碼；開始後是病房全景、監視器、指令橫幅、大字訊息流。沒有任何操作按鈕 |
| **教師** | `…/?m=teacher&k=金鑰` | 人數、情境設定、開始、暫停、加速、結束、報表、下載錄影，以及同一份 QR |
| **學員** | 掃 QR（或輸入網址 + 四位數房間代碼） | 自己的角色、自己的動作面板、只有自己看得到的私訊 |

### 步驟

1. 投影幕開 host 網址 → 出現大 QR code 與房間代碼。
2. 教師控制台先選 **這一場有幾個人（3–6）**。
   沒用到的角色不會出現在病房裡，報表也不會列，學員的位子也只開放這麼多個。
3. 學員掃 QR → **輸入一個字**（名字裡挑一個）→ 選顏色 → 進入。
   那個字就是他在投影幕上的代號。
4. 教師選 **點滴 / 起始心律 / 病因**，按「開始演練」。
5. 六個人（或 n 個人）從門口進場 —— 系統不會指派任何人任何事。
6. 要 pause-and-discuss 就按「⏸ 暫停」，所有人的畫面一起凍結。
7. 按「結束並看報表」。

### 報表

* 結果、閉環溝通與領導、心律判讀、壓胸品質、氣道、用藥、電擊安全、現場管理
* **泳道圖**：縱軸時間、橫軸每個人。壓胸是長橘條、找病因是紫、給藥是綠、倒地是紅；
  講話與下令是小菱形 —— 所以 Leader 那一欄會清楚看到他整場在說話而不是動手，
  誰整場沒事做也一目了然
* 用藥與電擊逐筆紀錄、關鍵事件時間軸、全場對話逐字稿
* 可以「複製報表文字」或「下載 .txt」帶走

---

## 手機怎麼操作

因為學員是**看投影幕、不是看手機**，操作設計成不用盯著螢幕也能做：

* **走路**：手指按住病房畫面的**任何一個位置**，往哪個方向拖就往哪裡走，放開就停。
  搖桿會長在你手指按下去的地方，所以不用找。
* **快速定位**：輕點某個站位或設備（不拖曳），就會自動走過去。
* **到站會震動**：走進床左、床右、床頭、床尾、門口或任何設備旁邊時，手機會震一下。
* **面板最上面一行**永遠寫著「你在：床左」「手上：留置針」，抬頭看完螢幕低頭一眼就知道。
* **動作分三層**：① 現在能做的處置 → ② 溝通 → ③ 準備與雜項。
  要救人的按鈕永遠在最上面。

---

## 重播

開 `…/?m=replay`，選伺服器上的錄影檔或從電腦上傳 `.json`。

錄影檔只有幾 KB —— 它記的是**隨機種子加上每一個操作**，播放時把整場重新算一次。
所以可以：

* 任意拉時間軸（往前往後都可以）
* 1× / 2× / 4× / 8× 變速
* **切換視角**：看任何一個人當時收到的私訊
* 隨時叫出報表

---

## 教案設計重點

| 機制 | 教什麼 |
|---|---|
| 系統不指派角色，Leader 要自己宣告、可以交接 | Role clarity / Leadership |
| Order 下達後別人要按「複誦」才算，報表算複誦率與延遲 | Closed-loop communication |
| 三條線索（病歷／家屬／超音波）只有查到的人知道，要按「📢 大聲喊出」 | Shared mental model |
| 只有站在電腦車前才看得到急救經過幾分幾秒 | Situational awareness |
| 電擊器與超音波的線不夠長，一定要有人去推機器 | Resource management |
| 留置針只有 3 根、一次只能拿一樣東西、壓胸會累 | Resource management |
| **摸脈搏的結果只有摸的人知道**，要喊出來團隊才會停手 | Closed-loop / ROSC 辨識 |
| NIBP 任何人在床邊都能量，結果全隊都看得到 | 客觀證據 vs 個人感受 |
| 心律檢查停 15 秒、跳出乾淨的大張心電圖，Leader 必須宣告可／不可電擊 | 心律判讀與決策 |
| 沒喊 CLEAR 就放電會電到人；有喊則多數人來得及放手 | 病人與團隊安全 |
| 家屬會靠近、拉住人、讓所有工作變慢 35% | 現場管理與家屬溝通 |
| CCF 全程隱藏，結束才公布 | 不能靠儀表板打，要靠團隊自己管 |

### 生理引擎（三件事決定病人活不活）

1. **灌流**：不壓胸時節律以 0.62/秒惡化，最好的壓胸只能回復 0.495/秒
   —— 整局是淨虧損的比賽。
2. **除顫**：可電擊節律有機率轉律（受焦耳數、CCF、Amiodarone、時間影響）。
   **轉律成功但病因沒解除 → 變成 PEA**，還是摸不到脈搏。
3. **病因**：五種可逆病因，處理正確後還要等 15–120 秒起效，期間 CCF 要 ≥ 50%。

ROSC 條件：病因已解除 **且** 管路正確 **且** 起效時間到 **且** CCF ≥ 50%
**且** 目前不是可電擊節律。
達成後病人生理上恢復循環，但**團隊不會自動知道** —— EtCO₂ 跳到 40、螢幕變規則心律，
要有人去摸脈搏並喊出來。沒發現而繼續壓胸 45 秒，病人會再度停止。

---

## 想改內容的話

都在 `ihca-sim.js` 最上面的 `K = {…}` 常數區：

| 要改什麼 | 改哪裡 |
|---|---|
| 病因、線索文字、正確處置、起效時間 | `K.CAUSES` |
| Order 選單的句子 | `K.ORDERS` |
| 各種回報台詞 | `K.READYFOR` |
| 家屬台詞 | `K.FAMLINES` / `K.ESCORTLINES` |
| 病房擺設與站位座標 | `K.ST` / `K.EQ0` |
| 插管成功率 | `ettFail()` |
| 電擊轉律機率 | `shockP()` |
| 疲勞、節律惡化速率、ROSC 條件 | `step()` 裡面 |

改完存檔，重新執行 `node server.js` 就生效（雲端版要重新上傳）。
